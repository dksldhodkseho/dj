package miniproject.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PublicationApprovedManagementRepository extends JpaRepository<PublicationApprovedManagement, Long> {
}
